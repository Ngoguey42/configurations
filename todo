


import initialization list
import non-pure member functions
import statics variables;

adaptative bindings with languages
redo all bindings

debug without new line
operator prototype template

put tabs until 28 or 32's column reached;

add all includes from current directory

new nested class

"std::string const &"
"std::"
"(void)|;"

#include <|> template
#include "|.hpp" template
